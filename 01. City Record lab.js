function solve(city, population, treasury) {
    let record = {
    };
    record.name = city
    record.population = population
    record.treasury = treasury

    return record
}


console.log('Tortuga',

7000,

15000);